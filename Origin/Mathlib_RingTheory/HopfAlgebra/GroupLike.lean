/-
Generated by origin.py from RingTheory/HopfAlgebra/GroupLike.lean
Genuine declarations: 2 of 5 total
Dissolved: 0
Infrastructure skipped: 3
-/
import Origin.Core

/-! # RingTheory.HopfAlgebra on Option α -/

universe u
variable {α : Type u}

def GroupLike.toUnits : GroupLike R A →* Aˣ where
  toFun a := {
    val := a
    inv := antipode R a
    val_inv := a.2.mul_antipode_cancel
    inv_val := a.2.antipode_mul_cancel
  }
  map_one' := by ext; rfl
  map_mul' a b := by ext; rfl

lemma IsGroupLikeElem.isUnit (ha : IsGroupLikeElem R a) : IsUnit a :=
  (GroupLike.toUnits R ⟨a, ha⟩).isUnit

