/-
Generated by origin.py from Order/TypeTags.lean
Genuine declarations: 3 of 10 total
Dissolved: 0
Infrastructure skipped: 7
-/
import Origin.Core

/-! # Order on Option α -/

universe u
variable {α : Type u}

def WithBot (α : Type*) := Option α

def some : α → WithBot α :=
  Option.some

def recBotCoe {C : WithBot α → Sort*} (bot : C ⊥) (coe : ∀ a : α, C a) : ∀ n : WithBot α, C n
  | ⊥ => bot
  | (a : α) => coe a

