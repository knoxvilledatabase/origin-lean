/-
Generated by origin.py from Order/Notation.lean
Genuine declarations: 6 of 7 total
Dissolved: 0
Infrastructure skipped: 1
-/
import Origin.Core

/-! # Order on Option α -/

universe u
variable {α : Type u}

class Compl (α : Type*) where
  /-- Set / lattice complement -/
  compl : α → α

class HasCompl (α : Type*) where
  /-- Set / lattice complement -/
  compl : α → α

class HImp (α : Type*) where
  /-- Heyting implication `⇨` -/
  himp : α → α → α

class HNot (α : Type*) where
  /-- Heyting negation `￢` -/
  hnot : α → α

class Top (α : Type*) where
  /-- The top (`⊤`, `\top`) element -/
  top : α

class Bot (α : Type*) where
  /-- The bot (`⊥`, `\bot`) element -/
  bot : α

