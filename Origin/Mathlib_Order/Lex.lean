/-
Generated by origin.py from Order/Lex.lean
Genuine declarations: 8 of 36 total
Dissolved: 0
Infrastructure skipped: 28
-/
import Origin.Core

/-! # Order on Option α -/

universe u
variable {α : Type u}

def Lex (α : Type*) :=
  α

def toLex : α ≃ Lex α :=
  Equiv.refl _

def ofLex : Lex α ≃ α :=
  Equiv.refl _

protected def Lex.rec {β : Lex α → Sort*} (h : ∀ a, β (toLex a)) : ∀ a, β a := fun a => h (ofLex a)

def Colex (α : Type*) :=
  α

def toColex : α ≃ Colex α :=
  Equiv.refl _

def ofColex : Colex α ≃ α :=
  Equiv.refl _

protected def Colex.rec {β : Colex α → Sort*} (h : ∀ a, β (toColex a)) : ∀ a, β a :=
  fun a => h (ofColex a)

