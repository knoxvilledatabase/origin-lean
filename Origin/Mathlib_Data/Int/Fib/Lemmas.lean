/-
Generated by origin.py from Data/Int/Fib/Lemmas.lean
Genuine declarations: 1 of 1 total
Dissolved: 0
Infrastructure skipped: 0
-/
import Origin.Core

/-! # Data.Int.Fib on Option α -/

universe u
variable {α : Type u}

lemma fib_natCast_succ_mul_fib_natCast_pred_sub_fib_natCast_sq (n : ℕ) :
    fib (n + 1) * fib (n - 1) - fib n ^ 2 = (-1) ^ n := by
  induction n with
  | zero => simp
  | succ _ _ => grind [fib_add_two]

