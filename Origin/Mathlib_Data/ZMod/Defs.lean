/-
Generated by origin.py from Data/ZMod/Defs.lean
Genuine declarations: 2 of 16 total
Dissolved: 1
Infrastructure skipped: 13
-/
import Origin.Core

/-! # Data.ZMod on Option α -/

universe u
variable {α : Type u}

def ZMod : ℕ → Type
  | 0 => ℤ
  | n + 1 => Fin (n + 1)

theorem card (n : ℕ) [Fintype (ZMod n)] : Fintype.card (ZMod n) = n := by
  cases n with
  | zero => exact (not_finite (ZMod 0)).elim
  | succ n => convert Fintype.card_fin (n + 1) using 2

