/-
Generated by origin.py from Data/Rat/Init.lean
Genuine declarations: 4 of 8 total
Dissolved: 0
Infrastructure skipped: 4
-/
import Origin.Core

/-! # Data.Rat on Option α -/

universe u
variable {α : Type u}

def NNRat := {q : ℚ // 0 ≤ q}

class NNRatCast (K : Type*) where
  /-- The canonical homomorphism `ℚ≥0 → K`.

  Do not use directly. Use the coercion instead. -/
  protected nnratCast : ℚ≥0 → K

def num (q : ℚ≥0) : ℕ := (q : ℚ).num.natAbs

def den (q : ℚ≥0) : ℕ := (q : ℚ).den

