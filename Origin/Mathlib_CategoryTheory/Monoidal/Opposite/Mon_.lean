/-
Generated by origin.py from CategoryTheory/Monoidal/Opposite/Mon_.lean
Genuine declarations: 2 of 6 total
Dissolved: 0
Infrastructure skipped: 4
-/
import Origin.Core

/-! # CategoryTheory.Monoidal.Opposite on Option α -/

universe u
variable {α : Type u}

def mopEquiv : Mon C ≌ Mon Cᴹᵒᵖ where
  functor :=
    { obj M := ⟨mop M.X⟩
      map f := ⟨f.hom.mop⟩ }
  inverse :=
    { obj M := ⟨unmop M.X⟩
      map f := ⟨f.hom.unmop⟩ }
  unitIso := .refl _
  counitIso := .refl _

def mopEquivCompForgetIso :
    (mopEquiv C).functor ⋙ Mon.forget Cᴹᵒᵖ ≅
    Mon.forget C ⋙ (MonoidalOpposite.mopEquiv C).functor :=
  .refl _

