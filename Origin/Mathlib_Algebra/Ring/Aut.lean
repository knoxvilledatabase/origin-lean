/-
Generated by origin.py from Algebra/Ring/Aut.lean
Genuine declarations: 5 of 12 total
Dissolved: 0
Infrastructure skipped: 7
-/
import Origin.Core

/-! # Algebra.Ring on Option α -/

universe u
variable {α : Type u}

abbrev RingAut := RingEquiv R R

def toAddAut : RingAut R →* AddAut R where
  toFun := RingEquiv.toAddEquiv
  map_one' := rfl
  map_mul' _ _ := rfl

def toMulAut : RingAut R →* MulAut R where
  toFun := RingEquiv.toMulEquiv
  map_one' := rfl
  map_mul' _ _ := rfl

def toPerm : RingAut R →* Equiv.Perm R where
  toFun := RingEquiv.toEquiv
  map_one' := rfl
  map_mul' _ _ := rfl

theorem coe_pow (f : R ≃+* R) (n : ℕ) : ⇑(f ^ n) = f^[n] := by
  induction n with
  | zero =>
    simp
  | succ n ih =>
    ext
    simp [pow_succ, ih]

