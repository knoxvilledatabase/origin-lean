/-
Generated by origin.py from Algebra/Ring/Rat.lean
Genuine declarations: 2 of 10 total
Dissolved: 2
Infrastructure skipped: 6
-/
import Origin.Core

/-! # Algebra.Ring on Option α -/

universe u
variable {α : Type u}

lemma num_div_den (r : ℚ) : (r.num : ℚ) / (r.den : ℚ) = r := by
  rw [← Int.cast_natCast, ← divInt_eq_div, num_divInt_den]

lemma natCast_eq_divInt (n : ℕ) : ↑n = n /. 1 := by rw [← Int.cast_natCast, intCast_eq_divInt]

