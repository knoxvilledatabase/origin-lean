/-
Generated by origin.py from Algebra/Category/GrpWithZero.lean
Genuine declarations: 1 of 13 total
Dissolved: 2
Infrastructure skipped: 10
-/
import Origin.Core

/-! # Algebra.Category on Option α -/

universe u
variable {α : Type u}

def Iso.mk {α β : GrpWithZero.{u}} (e : α ≃* β) : α ≅ β where
  hom := ofHom e
  inv := ofHom e.symm
  hom_inv_id := by
    ext
    exact e.symm_apply_apply _
  inv_hom_id := by
    ext
    exact e.apply_symm_apply _

