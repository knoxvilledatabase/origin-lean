/-
Generated by origin.py from Algebra/Module/Equiv/Opposite.lean
Genuine declarations: 2 of 10 total
Dissolved: 0
Infrastructure skipped: 8
-/
import Origin.Core

/-! # Algebra.Module.Equiv on Option α -/

universe u
variable {α : Type u}

theorem LinearMap.ext_ring_op
    {σ : Rᵐᵒᵖ →+* S} {f g : R →ₛₗ[σ] M} (h : f (1 : R) = g (1 : R)) :
    f = g :=
  ext fun x ↦ by
    rw [← one_mul x, ← op_smul_eq_mul, f.map_smulₛₗ, h, g.map_smulₛₗ]

def opLinearEquiv : M ≃ₗ[R] Mᵐᵒᵖ :=
  { opAddEquiv with map_smul' := MulOpposite.op_smul }

