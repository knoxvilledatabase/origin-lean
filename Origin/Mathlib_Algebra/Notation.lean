/-
Generated by origin.py from Algebra/Notation.lean
Genuine declarations: 4 of 4 total
Dissolved: 0
Infrastructure skipped: 0
-/
import Origin.Core

/-! # Algebra on Option α -/

universe u
variable {α : Type u}

class PosPart (α : Type*) where
  /-- The *positive part* of an element `a`. -/
  posPart : α → α

class OneLePart (α : Type*) where
  /-- The *positive part* of an element `a`. -/
  oneLePart : α → α

class NegPart (α : Type*) where
  /-- The *negative part* of an element `a`. -/
  negPart : α → α

class LeOnePart (α : Type*) where
  /-- The *negative part* of an element `a`. -/
  leOnePart : α → α

