/-
Generated by origin.py from GroupTheory/GroupAction/SubMulAction/Pointwise.lean
Genuine declarations: 2 of 12 total
Dissolved: 2
Infrastructure skipped: 8
-/
import Origin.Core

/-! # GroupTheory.GroupAction.SubMulAction on Option α -/

universe u
variable {α : Type u}

theorem subset_coe_one : (1 : Set M) ⊆ (1 : SubMulAction R M) := fun _ hx =>
  ⟨1, (one_smul _ _).trans hx.symm⟩

theorem mem_mul {p q : SubMulAction R M} {x : M} : x ∈ p * q ↔ ∃ y ∈ p, ∃ z ∈ q, y * z = x :=
  Set.mem_mul

